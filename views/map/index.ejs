<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/static/images/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="/static/css/style.css">
    <link rel="stylesheet" href="/static/css/leaflet/leaflet.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
    <script src="/static/js/leaflet/leaflet.js"></script>
    <title>Beat Saber FR - Carte interactive</title>
</head>
<body>
    <div class="background"><div class="blur"></div></div>
    
    <div class="container">
        <form class="form">
            <div class="card card-border">
                <div class="card-title">Carte interactive</div>
                <div class="card-body">
                    <% if(!user.isBSFR) { %>
                    <p>
                        Vous devez être membre du serveur Discord BSFR afin de pouvoir visualiser la carte interactive.
                    </p>
                    <p>
                        Vous pouvez nous rejoindre en cliquant sur ce lien d'invitation : <a href="<%= process.env.DISCORD_INVITATION_URL %>" target="_blank"><%= process.env.DISCORD_INVITATION_URL %></a>
                    </p>
                    <p>
                        Actualisez la page après avoir rejoint le serveur Discord pour voir la carte.
                    </p>
                    <% } %>

                    <hr>

                    <span>Connecté en tant que : <a href="https://discordapp.com/users/<%= user.id %>" target="_blank"><%= user.username %>#<%= user.discriminator %></a> - <a href="/discord/logout">Se déconnecter</a></span>
                </div>
            </div>

            <% if(user.isBSFR) { %>
            <div class="card">
                <div class="card-body">
                    <div id="map" style="width: 100%; height: 600px; position: relative; outline: none;" class="leaflet-container leaflet-touch leaflet-fade-anim leaflet-grab leaflet-touch-drag leaflet-touch-zoom" tabindex="0"></div>
                </div>
            </div>
            <% } %>
        </form>
    </div>

    <script src="/static/js/app.js"></script>
    <% if(user.isBSFR) { %>
    <script src="/static/js/map.js"></script>
    <% } %>

    <% if(typeof error !== 'undefined' || typeof success !== 'undefined') { %>
    <script>
        showAlert(<%= typeof error !== 'undefined' ? 'false' : 'true' %>, '<%= typeof error !== 'undefined' ? error : success %>')
    </script>
    <% } %>
</body>
</html>